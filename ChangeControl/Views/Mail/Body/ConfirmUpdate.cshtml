@{
  string host = $"/Detail/Index/?id={Model.Code}";

  var Irev = Convert.ToInt32(Session["ConfirmRev"].ToString());
  int Iprev_rev = Irev - 1;
  string rev = Irev.ToString("00");
  string prev_rev = Iprev_rev.ToString("00");
}
    <p>Dear all concernings,</p>
    <p>
      According to  <b>@Model.Code</b> of model <b>@Model.Model</b> Part no. <b>@Model.PartNo</b>, Confim data has been revised
       from revision <b>@prev_rev</b> to revision <b>@rev</b>
       , by <b>@Session["FullName"].ToString()</b>, Please check your confim data again.
    </p>
    <p>For more details, please click this <a href="@ViewBag.Url">Link.</a></p>
    <p>Best Regards</p>