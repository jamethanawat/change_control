@{
  string host = $"/Detail/Index/?id={Model.Code}";

  @* var rev = (int) Session["TrialRev"];
  int prev_rev = rev - 1; *@

    var Irev = Convert.ToInt32(Session["TrialRev"].ToString());
  int Iprev_rev = Irev - 1;
  string rev = Irev.ToString("00");
  string prev_rev = Iprev_rev.ToString("00");
}
    <p>Dear all concernings,</p>
    <p>
      According to  <b>@Model.Code</b> of model <b>@Model.Model</b> Part no. <b>@Model.PartNo</b>, Trial data has been revised
       from revision <b>@prev_rev</b> to revision <b>@rev</b>
       , by <b>@Session["FullName"].ToString()</b>, Please check your trial data again.
    </p>
    <p>For more details, please click this <a href="@ViewBag.Url">Link.</a></p>
    <p>Best Regards</p>